/**
    Replace with .env in future if possible
 */
enum Endpoints {
    SHORTENER_URL = "http://localhost:8080/",
    API_URL = "http://localhost:8080/",
    PATH_LOGIN = "login",
    PATH_LINKS = "api/links",
    PATH_USERS = "json_users",
    PATH_SCHEDULER = "renew"
}

export default Endpoints;